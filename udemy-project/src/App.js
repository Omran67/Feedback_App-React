function App() {
    return (
        <div>
            <h1> My App</h1>
            <h3> JSX Rules</h3>
            <p> JSX must have only one parent element</p>
            <p> JSX must have only one parent element</p>
        </div>
    )
}

export default App;